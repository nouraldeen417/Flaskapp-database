# apiVersion: apps/v1
# kind: StatefulSet
# metadata:
#   name: mysql
#   namespace: default
# spec:
#   replicas: 2
#   selector:
#     matchLabels:
#       app: mysql
#   serviceName: mysql-svc
#   podManagementPolicy: OrderedReady
#   updateStrategy:
#     type: RollingUpdate
#   template:
#     metadata:
#       labels:
#         app: mysql
#     spec:
#       terminationGracePeriodSeconds: 10
#       containers:
#         - name: mysql
#           image: nouraldeen152/mysql:latest
#           ports:
#             - name: mysql
#               containerPort: 3306
#               protocol: TCP
#           env:
#             - name: MYSQL_ROOT_PASSWORD
#               valueFrom:
#                 secretKeyRef:
#                   name: mysql-secret
#                   key: MYSQL_ROOT_PASSWORD
#           volumeMounts:
#             - name: mysql-data
#               mountPath: /var/lib/mysql
#   volumeClaimTemplates:
#     - metadata:
#         name: mysql-data
#       spec:
#         accessModes: ["ReadWriteOnce"]
#         storageClassName: nfs-storage
#         resources:
#           requests:
#             storage: 5Gi
# # apiVersion: mysql.oracle.com/v2
# # kind: InnoDBCluster
# # metadata:
# #   name: mysql-cluster
# #   namespace: default
# # spec:
# #   # The Operator strictly requires this field to handle all credentials.
# #   # It will automatically map the keys inside to the right environment variables.
# #   secretName: mysql-secret 
  
# #   tlsUseSelfSigned: true
  
# #   # Must be 3 for high availability and failover quorum
# #   instances: 3 
  
# #   # Forcing your custom image
# #   baseServerImage: nouraldeen152/mysql:latest
  
# #   # Your exact PVC specifications translated for the Operator
# #   datadirVolumeClaimTemplate:
# #     accessModes:
# #       - ReadWriteOnce
# #     storageClassName: nfs-storage
# #     resources:
# #       requests:
# #         storage: 5Gi

# apiVersion: mysql.oracle.com/v2
# kind: InnoDBCluster
# metadata:
#   name: mysql-cluster
#   namespace: default
# spec:
#   secretName: mysql-secret 
#   tlsUseSelfSigned: true
#   instances: 3 
#   edition: community
#   podSpec:
#     labels:
#       app: mysql
#     containers:
#       - name: mysql
#         image: nouraldeen152/mysql:latest
#   datadirVolumeClaimTemplate:
#     accessModes:
#       - ReadWriteOnce
#     storageClassName: nfs-storage
#     resources:
#       requests:
#         storage: 5Gi